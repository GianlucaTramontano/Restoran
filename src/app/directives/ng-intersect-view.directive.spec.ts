import { NgIntersectViewDirective } from './ng-intersect-view.directive';

describe('NgIntersectViewDirective', () => {
  it('should create an instance', () => {
    const directive = new NgIntersectViewDirective();
    expect(directive).toBeTruthy();
  });
});
